# Docker specific files
Dockerfile
docker-compose.yml
.dockerignore

# Git specific files
.git
.gitignore

# Node modules (we install them inside the container)
node_modules

# NestJS build output (we build inside the container)
dist

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables file (should be passed via compose)
.env

# OS generated files
.DS_Store
Thumbs.db
